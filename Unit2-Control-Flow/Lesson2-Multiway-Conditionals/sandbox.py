"""Lesson 2 Sandbox
CS100: Roadmap to Computing
"""

# ---------- Break it, then fix it ----------
# This chain is correct. Run it with level = 499, then level = 500.

level = 499

if level >= 500:
    rank = "Silver"
else:
    rank = "Bronze"

print(f"Level {level} -> {rank}")

# Now change the >= to a plain > and run both numbers again.
#
#   Which number changed its answer?
#   Which number did not?
#
# Change it back to >= before you start the activity.
